project project;
const
  len=100;
var
  total:int;
  
routine compute;
  
  var
    num:int;
start
  input(x);
  num:=n+10;
  output(num);
end;
  
start
  input(i);
  if (i< 100) then
    j:=5
  endif;
  loop (i<> min) do
    start
      i := i+1;
      outputt(i);
      total :=total+i;
    end;
  output(total);
end.